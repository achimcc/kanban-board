enum TaskStatus {
    ToDo = "TO DO",
    Doing = "DOING",
    Done = "DONE"
}

enum TaskActions {
    Create = "CREATE",
    SetStatus = "SET_STATUS",
    Delete = "DELETE"
}

export {TaskStatus, TaskActions}